# Common Role

This role installs and configures some common tools that are used during any assessment such as cli tools like `tmux`, `tree` and `searchsploit` as well as some common configuration files for those tools.
